.Ao entrar na atividade (default) tem que ser atividade do diaAberto atual.
.Só se pode duplicar atividades de anos passados e sem roteiros (e duplica-se sempre para o ano atual)
    .O botão só de duplicar só aparece:
        .Atividade de anos passados.
        .Sem haverem no diaAberto atual
        .Sem estarem associadas a roteiros.
        .Deverão estar no estado especifico (ex: arquivado) ????

.Ao clicar pop up a pedir confirmação?????


//vou ignorar esta parte
.Atualizar base de dados:
    .Atualmente ao fazer isto iria aparecer atividades repetidas na base de dados para dias aberto diferentes (é uma coluna da tabela atividade)
        Solução atividade não pode estar associada a um dia aberto (pelo menos não na tabela)
        .Possivel fix passar a existir uma tabela chamada sessaoAtividade -> Com duas colunas DiaAberto e Atividade
            Assim na parte das atividades aparecerá sessaoAtividade